name: 🚀 CI Pipeline

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

permissions:
  contents: read

jobs:
  validate-baseline:
    name: 🔍 Baseline validation
    uses: ./.github/workflows/baseline-validation.yml
    secrets: inherit
